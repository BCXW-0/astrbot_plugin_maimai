# 更新日志

## 2.5.0（2026-08-05）

- 优化打标签模型，提高准确率。
- 标签训练审核改为当前对话模型与 AstrBot `google_gemini/gemini-2.5-pro` 双模型严格一致后再进入联网校验；历史三模型一致记录继续兼容保留。
- 拒绝带工具调用标记、Markdown 包裹或额外说明文字的审核响应，避免非 JSON 内容进入训练元数据。
- 修正训练目标误用定数裁剪展示标签的问题，改用双模型共识并按谱面特征分数限制为最多 5 个训练标签；定数占比裁剪不再制造伪负样本。
- 增加验证集与留出集质量门禁，并移除绕过逐标签校准阈值的运行时兜底输出；低置信度谱面保持无标签，避免误报。
- 重新生成 125 条训练元数据、完整来源/标签位置映射、Loss 曲线和本地模型；本次留出集 precision 96.30%，F1 68.42%。

## 2.4.0（2026-08-04）

- 优化谱面标签分析：复用谱面事件分组、窗口和特征计算，完整分析保留原有窗口、撞尾候选、标签位置与映射输出。
- 新增轻量标签分析路径，搜索和锐评 B50 只计算所需标签摘要；查看详情时仍按原方式生成完整证据。
- 将谱面目录索引改为文件签名增量更新，并使用元数据解析器避免索引阶段构建全部音符事件；变速 BPM、定数和谱面映射字段与完整解析保持一致。
- 增加结果缓存上限、同谱面并发分析合并和同文件谱面解析缓存，避免长期运行时结果和解析对象无界增长。
- 锐评 B50 的谱面标签上下文移出事件循环；OneCat 响应改为分块读取并增加大小保护；WebUI 状态接口失败后自动退避重试。
- 保持现有指令、WebUI 操作、接口路径和用户端输出字段不变；本轮没有指令变化，`static/help.txt` 保持不变。

## 2.3.0（2026-08-03）

- 清理不再维护的成长辅助、目标查询和引导入口，移除对应处理器、数据文件、WebUI 指令清单与帮助文案。
- 保留 `吃分推荐` 的目标 Rating 参数，清理目标参数入口留下的独立转发和绘图接口。

## 2.2.0（2026-08-03）

- 按 `maimai.xls` 的候选特征、难点特征和同定数占比上限，强制重算全部 `12.6 - 15.0` 的 Expert / Master / Re:Master 难度，并生成完整训练元数据、候选/最终使用率和逐谱面报告。
- 训练全量本地多标签模型，保存压缩训练元数据、谱面清单、审计记录、Loss 曲线、训练记录、模型权重和模型元数据。
- 移除持久化谱面标签库：推荐、锐评 B50 和 WebUI 标签详情统一按 `shortid:level_index` 查找 `static/Levels`，解析对应 `inote` 后直接调用本地模型；标签结果只保存在进程内存中。
- `static/Levels` 改为管理员在 WebUI 首次使用时下载的本地输入，加入 Git 忽略规则；训练元数据和模型文件仍可作为仓库静态资源提交。
- 分析与下载任务改为后台内存态任务，进度只更新状态区域；新增“插件日志”WebUI 模块，仅展示本插件日志并支持筛选、自动刷新和清空。
- 整理谱面标签数据流、训练字段和 WebUI 说明；`static/help.txt` 本轮无指令变化，保持不变。

## 2.1.3（2026-08-02）

- 修复 WebUI 谱面下载进度显示为 `0 / 0` 的问题：下载任务统一返回 `total` 进度总数，并兼容旧状态中的 `selected` 字段。
- 下载任务启动时先写入可轮询的初始进度状态，完成 OneCat 筛选后立即显示候选歌曲数量与处理进度。

## 2.1.2（2026-08-02）

- 整理 WebUI 谱面标签接口和后台任务状态，统一本地模型分析、检索和任务控制。
- 将 `/api/chart_tags/*` 统一为本地模型谱面标签接口，保留下载、定数筛选、强制重算、新谱计算、结果检索、详情和谱面文件映射。
- 清理谱面标签页面的过时说明和旧管理区，当前 WebUI 只展示本地模型分析结果；`static/help.txt` 本轮保持不变。

## 2.1.1（2026-08-02）

- 将 WebUI 的「自动打标」与「谱面标签」合并为单一「谱面标签」模块，保留本地模型分析和检索功能。
- 修复自动打标请求在 Token 含特殊字符、反向代理或后端异常时显示“请求失败”的问题：前端改用编码后的 URL 与 `X-Access-Token`，后端统一返回 JSON 错误信息。
- 清理 README 中重复的 WebUI 模块说明，并同步 `static/help.txt` 的 WebUI 功能名称；没有新增机器人指令。

## 2.1.0（2026-08-02）

- 新增独立 WebUI「自动打标」功能块：下载谱面、调用本地标签模型分析和检索打标结果均不注册机器人指令。
- 下载支持 10.0 - 15.0 定数滑条、全部下载/仅未下载/搜索下载；只保留 OneCat 返回的 `.txt` 谱面，排除 BGA、音频、其他资源和宴谱。
- 新增本地模型的强制重算与新谱计算任务，保存模型标签、概率、局部窗口、源文件哈希和分析状态。
- 建立标签文件、谱面文件和谱面信息的双向稳定映射：每条记录显式保存 `tag_file`、`tag_file_key`；`chart_mapping` 按 `shortid:level_index` 定位单个难度，`chart_file_mapping` 按谱面文件列出全部难度；每条记录对应 `diff_id`、`&inote_N`、相对文件路径、文件 SHA-256 与曲名/艺术家/定数/BPM/谱师信息。
- 下载完成后先建立待分析映射条目；模型分析只更新对应条目，WebUI 支持按 ID、曲名、艺术家、谱师和定数查询。

## 2.0.0（2026-08-02）

- 新增本地 maidata 解析、两小节结构窗口和撞尾审计，记录完整谱面内容、BPM 段、标签位置及源文件哈希。
- 按 simai 语法和撞尾资料整理 Slide 末端路径区的时序证据，并排除 Ex 目标和普通 Hold 重叠误判。
- 建立本地模型训练数据集、Loss 记录、模型权重和审阅报告，为后续全量重算提供基础。

## 1.8.1（2026-07-26）

- 校准谱面结构标签定义：**管子 = hold**（短 hold 局部过密 / hold 链式紧接），不再把滑键当管子。
- 收紧 **双押**：非重叠 1 秒窗口的同时击绝对峰值、热窗占比与链式，避免 `multi_ratio=1.0` 误标。
- 收紧 **定位**：非重叠高密 + 短间隔大位移（卡手）/ 难星局部，降低普通高难谱误伤。
- 高置信结构标签保留主配置顺序；管子权重上调并从泛化标签集合移除。
- 收紧结构标签的局部窗口判定并统一标签别名。

## 1.8.0（2026-07-26）

- 新增本地 `maidata` 解析器和可解释结构特征引擎，支持 simai 音符、Hold、Slide、触摸、BPM 变化和局部密度分析。

## 1.7.3（2026-07-25）

- 谱面标签支持难点权重：辨识度高的配置（死镰/如龙/秒划/手序/飞手等）权重大，底力/手速等泛化标签权重低。
- 标签结果输出 `tag_scores`，展示与吃分推荐按权重排序取舍。
- 更新 `static/help.txt` 为非管理员指令触发说明；同步精简 README。

## 1.7.2（2026-07-22）

- 重构「锐评b50」含金量：以水鱼 `chart_stats.fit_diff`（拟合定数相对官定 ds）为底座，叠加达成质量、B15 宽容、低样本降权与地板水谱惩罚。
- 含金量标签扩展为：特别高 / 偏高 / 正常 / 含水量较高 / 很高 / 未知；角色标记支持难谱、水谱、鸟/鸟加、地板、未满鸟吃分。
- 压缩 LLM 证据包与 SYSTEM 提示词：规则进 system，用户侧只保留结构化 B50 快照与 TOP 列表，降低冗余注入。
- 段位提示按 dx2026 国服常用分段细化，锐评语气对齐圈内黑话（推分/挖分/蹭定数/擦地板等）。

## 1.7.1（2026-07-22）

- 移除上述独立管理指令的更新逻辑，保留兼容入口用于提示统一使用 `舞萌初始化`。
- 统一由 `舞萌初始化`（别名 `一键更新舞萌`）一次性执行上述四项更新并热加载。
- 输入旧四项指令时提示改用 `舞萌初始化`。
- 删除无用配置 `init_include_tables` 与死代码帮助常量；同步精简 README / WebUI / 帮助文案。

## 1.7.0（2026-07-22）

免费体验升级：同时强化管理员开箱能力与玩家成长闭环。

### 管理员 / 运维
- 新增 `舞萌体检` / `舞萌状态`：曲库、别名、表图、Token、Playwright、WebUI 一页体检。
- 新增 `舞萌初始化` / `一键更新舞萌`：串行更新曲库、别名、定数表、完成表并热加载运行态。
- `更新maimai数据` / `更新别名库` 改为热加载（曲库/牌子/猜歌/别名立即生效，无需整插件重载）。
- 每日定时维护增强：可自动刷新别名；定数表/完成表缺失时按配置自动补全。
- 新增配置：`daily_update_alias`、`daily_update_tables_if_empty`、`daily_update_hour`、`init_include_tables`。

### 玩家体验
- `帮助` 升级为文字高频指令 + 可选帮助图；支持 `帮助 查分/推分/同步/猜歌/管理`。
- 新增 `我的舞萌`：Rating、B35/B15 合计与地板、SSS+、绑定状态、下一首建议。
- 推分推荐支持目标 Rating。

## 1.6.0（2026-05-30 13:30）

- 将 `更新b50` 的 SGWCMAID 默认有效窗口从 180 秒调整为 600 秒。
- 优化插件重载后的资源初始化，统一图片资源加载入口，避免首次触发分数列表等图片命令时底图未加载。
- 新增 Playwright Chromium 启动检查，`ginfo` 环境缺失时返回可读提示，不再直接抛出后端异常。
- 优化等级进度和分数线指令的输入容错，减少无回复和误记错误日志的情况。
- 完成重载后指令烟测，覆盖无回复兜底、管理员分支和核心图片指令。

## 1.5.0（2026-05-26 17:40）

- 优化查分与别名接口请求会话复用，减少高频命令网络连接开销
- 优化曲库 ID/标题索引、谱面标签读取缓存和字体缓存，降低查询与图片生成开销
- 修正搜索分页总页数在整页结果下多显示一页的问题
- 将谱面标签任务状态文件改为运行期文件，避免任务停止后持续污染工作区

## 1.4.1（2026-05-24 01:37）

- “推分建议”推送逻辑优化
- 新增“吃粪推荐”反向推荐指令
- 优化推荐池标签倾向匹配与避让逻辑

## 1.4.0（2026-05-23 23:04）

- 优化插件启动进程

## 1.3.1（2026-05-23 21:10）

- 添加存储用户数据的json文件读取兜底机制，优化 I/O 开销。
- 修复 `更新b50/导` 大小写和冒号形式不易触发的问题，优化 SGID 提取，并避免系统 socks 代理环境缺少 `socksio` 时阻断同步。
- 修复 `更新b50/导` 发送处理中提示后被 AstrBot 消息发送阶段截断，导致最终同步结果不返回的问题。

## 1.3.0（2026-05-22 01:13）

- 修复 P1 安装风险：补充 `APScheduler` 到 `requirements.txt`，避免纯净环境安装依赖后因缺少定时任务库导致插件导入失败。
- 修复 P1 成绩同步依赖风险：`更新b50/导` 代码要求 `maimai-py>=1.4.2`、`maimai-ffi>=0.7.0`，现已将 `requirements.txt` 与运行期检查对齐，避免已安装旧版依赖时 `pip install -r requirements.txt` 不升级导致同步功能持续不可用。

## 1.2.2（2026-05-22 00:06）

- 优化“吃分推荐”推送逻辑：从按规则排序后的前最多 10 个候选谱面中随机推荐；候选不足 10 首时按实际候选数量抽取，候选为 0 时直接返回无候选提示。

## 1.2.1（2026-05-21 18:38）

- 优化“吃分推荐”输出：移除单独处理中提示，将推荐理由和曲目信息图合并为一条引用回复。
- 新增按用户分装的最近 3 次推荐谱面栈；候选超过 3 首时优先避开最近已推荐谱面。
- 推荐理由移除筛选过程说明，新增 `b50倾向` 标签统计。
- 添加“定拍”标签，优化标签映射。

## 1.2.0（2026-05-21 17:10）

- 优化“吃分推荐”响应流程：立即返回处理中提示，将候选计算和谱面详情图生成移出主事件循环，并限制并发请求，降低短时间触发导致 AstrBot 无响应的风险。
- 调整“吃分推荐”筛选规则：按当前 Rating 推导推荐定数区间，定数范围为 `当前 Rating / 1130` 到 `当前 Rating / 1075`，依次判断 B35/B15 地板、SSS+ 理论单曲 Rating、B50 中已 SSS+ 谱面排除，并优先推荐实际定数更低的谱面，其次推荐“拟合定数 - 实际定数”更低的谱面。
- B35/B15 地板只统计已有正单曲 Rating，忽略分值为 0 的占位成绩。

## 1.1.3（2026-05-21 15:38）

- 修复 WebUI 已配置 Token 后，公网或 NAT 访问根路径仍容易表现为“拒绝启动 / 403”的问题；现在访问根路径会显示 Token 输入页，接口仍保持 Token 校验。
- 优化了“谱面标签”的获取逻辑。
- 修复了牌子数据接口读取异常的Bug。

## 1.1.2（2026-05-20 16:04）

- WebUI 外部监听必须配置访问 Token；未配置 Token 时仅允许本机访问。
- 插件初始化改为分项降级，曲库、别名或牌子数据加载失败时不阻断命令注册和定时更新。
- 搜索、分数线和猜歌图片改为直接发送 base64 图片组件，减少临时文件残留。
- 插件停止时会清理猜歌后台任务、WebUI 启动任务和谱面标签更新任务。

## 1.1.1（2026-05-20 00:45）

首个发行版本，提供面向 AstrBot 的舞萌 DX 查询与管理能力：歌曲查询、成绩查询、B50 图片、B50 锐评、成绩同步、谱面标签、吃分推荐、猜歌和插件 WebUI。

安装、配置、静态资源准备和完整命令说明请查看 `README.md`。
